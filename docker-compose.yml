version: "3"

networks:
  dev:
    driver: bridge
    name: webnet

services:
  nginx:
    image: nginx:stable-alpine
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - "./nginx.conf:/etc/nginx/nginx.conf"
    depends_on:
      - backend
      - frontend
    networks:
      - dev

  backend:
    build:
      context: ./server
    networks:
      - dev
  
  frontend:
    build:
      context: ./client
    networks:
      - dev